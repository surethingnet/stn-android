# SureThingNet: My Home Network Security App

This directory is the development environment for the **SureThingNet** Android application.

It is ignored by the workspace meta repository as it is managed as a separate Git repository/worktree.
